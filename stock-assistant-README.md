# 📈 주식투자 AI 비서

브라우저에서 바로 쓰는 개인용 주식 투자 보조 도구입니다. 서버·회원가입 없이, 모든 데이터는 사용자의 기기(브라우저 localStorage)에만 저장됩니다.

## 시작하기

`stock-assistant.html` 파일을 브라우저로 열기만 하면 됩니다. (더블클릭 또는 브라우저에 드래그, 혹은 GitHub Pages로 배포된 주소로 접속)

## 기능

| 탭 | 내용 |
| --- | --- |
| 포트폴리오 | 한국(코스피/코스닥)·미국 종목을 등록하고 현재가·등락률·평가손익을 확인 |
| 시그널 | 등록한 종목 중 장대양봉(매수 관심) / 장대음봉(매도·손절 검토) 조건 충족 종목 표시 |
| 뉴스 | 등록한 종목의 최신 뉴스 헤드라인 조회, AI 키가 있으면 3줄 요약 + 투자심리 표시 |
| AI 비서 | 포트폴리오 맥락을 참고해 자유롭게 질문하고 답변받는 채팅 |
| 설정 | Anthropic API 키/모델 설정, 데이터 초기화 |

## 시세 데이터

Yahoo Finance의 공개 차트 API를 브라우저에서 직접 호출합니다. 별도의 서버나 API 키가 필요 없지만, 네트워크 환경에 따라 요청이 차단될 수 있습니다 — 그런 경우 새로고침을 다시 시도하거나 매수단가만 입력해 포트폴리오 기록용으로 사용할 수 있습니다.

시그널 탭은 **등록한 종목에 한해서만** 당일 캔들 조건(등락률 · 몸통비율)을 검사합니다. 코스피·코스닥 전 종목을 스캔하려면 이 저장소의 `stock_monitor.py` CLI 도구를 사용하세요.

## AI 기능 (선택)

뉴스 요약과 AI 질의응답은 [Anthropic API 키](https://console.anthropic.com/)가 있어야 동작합니다.

- 설정 탭에서 키를 입력하면 **이 브라우저에만** 저장됩니다.
- API 요청은 브라우저에서 Anthropic 서버로 **직접** 전송되며, 그 외 어떤 서버도 거치지 않습니다.
- 키를 입력하지 않아도 포트폴리오·시그널·뉴스 헤드라인 조회 기능은 그대로 사용할 수 있습니다.

⚠️ AI 답변은 투자 참고용 정보이며 투자 조언이 아닙니다. 최종 투자 판단과 책임은 본인에게 있습니다.

## 홈 화면에 추가 (앱처럼 사용)

- **Android (Chrome)**: 접속하면 상단에 "추가" 안내가 뜹니다. 또는 메뉴(⋮) → "홈 화면에 추가".
- **iPhone (Safari)**: 하단 **공유** 버튼 → **"홈 화면에 추가"**.

## 개인정보

- 포트폴리오·설정·대화 기록은 기기 내부(localStorage)에만 저장됩니다.
- 시세 조회(Yahoo Finance), 뉴스 조회(Google 뉴스), AI 요청(Anthropic)은 각 서비스로 직접 전송되며, 별도의 자체 서버로 수집·저장하지 않습니다.
- "설정 → 모든 데이터 삭제"로 언제든 초기화할 수 있습니다.

## 참고

투자 손실에 대한 책임은 본 도구 및 제작자에게 있지 않습니다. 투자 결정 전 반드시 본인의 판단과 추가 검증을 거치시기 바랍니다.
