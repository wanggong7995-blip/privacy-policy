# 클로드를 주식 투자에 쓰는 방법 — 이 저장소 기준 설계도

이 저장소에 이미 있는 두 가지 위에 얹는 것을 전제로 정리했다.

- `stock_monitor.py` — pykrx 일봉에서 거래대금 2000억↑ + 장대양봉(등락률 3%↑, 몸통비율 50%↑) 종목 필터
- `youtube_daily.py` + `.github/workflows/youtube-daily-summary.yml` — 채널 자막 → Claude 요약 → `summaries/` 자동 커밋

## 0. 역할을 자르는 선

| 계층 | 담당 | 하는 일 |
|---|---|---|
| 데이터 | pykrx / OpenDART / pandas | 시세·재무·수익률·상관계수 **계산**. 숫자는 모델에게 묻지 않는다 |
| 해석 | claude-opus-5 | 공시·뉴스·자막·사업보고서를 읽고 **분류·요약·근거 인용·이상징후 서술** |
| 결정 | 사람 | 진입·비중·손절. 모델 출력은 후보와 근거일 뿐 신호가 아니다 |

## Tier 1 — 기존 코드 확장 (각 1~2일)

### 1. 급등 사유 판별기
스캐너는 "무엇이 올랐는지"만 안다. "왜 올랐고 내일도 유효한가"는 캔들이 아니라 뉴스·공시 본문에 있다.

```
stock_monitor 결과(N종목) → 당일 뉴스·공시 수집(코드) → Claude 판정 → 표 출력
출력: {ticker, 사유: 실적|수주|신약·기술|테마·정책|수급·경영권|불명,
       지속성: 1-5, 근거인용, 확인필요}
```

- 원문 문장을 인용 못 하면 반드시 **사유 불명**을 반환하게 강제. 근거 없는 급등은 채울 빈칸이 아니라 걸러낼 대상이다.
- 재사용: `analyze()` 결과 DataFrame, `youtube_daily.py`의 `_call_claude()` 패턴, Actions 크론(07:10 UTC = 16:10 KST).
- 함정: 뉴스를 모델에게 "검색해 오라"고 시키지 말 것. 수집은 코드가, 판정만 모델이.

### 2. 유튜브 픽 스코어보드 (가장 값어치 있는 확장)
`summaries/`에 쌓이는 글은 읽고 잊힌다. 언급 종목을 구조화해 뽑아내면 "그 채널이 3개월 전 말한 것들이 실제로 어떻게 됐나"를 자동 채점할 수 있다.

```
summaries/*.md → Claude 2차 패스(종목·톤·근거·시계 추출) → picks.csv 누적
              → pykrx로 D+1 / D+5 / D+20 / D+60 수익률 채점 → 채널별 적중률
```

- 필드: `{날짜, 채널, 종목명, 티커, 톤: 긍정|중립|부정, 근거요약, 시계}`. 티커는 pykrx 종목 목록으로 대조해 오탈자를 거른다.
- 알파가 없다는 결론이 나와도 그 자체가 큰 소득이다.
- 함정: 단순 언급과 추천을 구분할 것. 톤 필드 없이 세면 지수 언급까지 픽으로 잡힌다.

### 3. 매매 일지 코치
개인 투자에서 가장 확실한 개선은 종목 발굴이 아니라 자기 패턴 교정이다.

```
매수 전 3줄(가설 / 무효화 조건 / 보유기간) + 체결 내역 → 주간 Claude 대조
→ 반복 패턴 진단: 손절 미이행 · 물타기 · 뉴스 추격 · 무효화 조건 발생 후 보유
```

- 프롬프트 축: "매매를 평가하지 말고 **기록된 계획과 실제 행동의 차이**만 지적하라." 결과가 좋았어도 계획을 어겼으면 지적 대상.
- 함정: 일지가 사후 변명이 되면 무의미. 진입 전 작성 시각이 남게 할 것.

## Tier 2 — 새 데이터 소스 (각 2~4일)

### 4. DART 공시 감시 + 영향도 등급
개인 손실은 예측 실패보다 공시를 늦게 읽어서 생긴다.

```
OpenDART 공시 폴링 → 관심종목 필터(룰) → Claude가 본문 조건 해석
→ 강한악재 / 약한악재 / 중립 / 호재 + 한 줄 이유
```

- 공시 **유형**은 룰이, 같은 유형 안의 **강도**는 모델이 판단(같은 유상증자라도 시설투자와 운영자금은 다르다).
- 함정: 등급을 매매 신호로 바로 연결하지 말 것. 확인 우선순위용이다.

### 5. 재무 이상징후 리포트
숫자는 pandas가 정확히 낸다. 놓치는 건 조합의 의미다.

```
코드: 매출채권회전율 · 재고 증가율 · 영업이익 대비 영업CF · 차입금 만기
→ Claude: 조합의 의미 서술 + 의심 순위 + 확인할 주석 항목
```

- 절대 규칙: 합산·비율 계산을 모델에 맡기지 않는다. 계산된 표만 넘긴다.
- 함정: 업종별 정상 범위가 다르다. 동종업계 중앙값을 함께 넣어야 오탐이 준다.

### 6. 전략 → 백테스트 변환기
Claude Code가 가장 잘하는 일이고, 첫 검증 대상은 이미 쓰는 조건이어야 한다.
**"거래대금 2000억 + 장대양봉 다음 날 시가 매수, 5일 보유"의 실제 기대값은?**

- 필수 포함: 거래비용, 슬리피지, 상하한가·거래정지 처리, 상장폐지 종목 포함(생존 편향 제거).
- 파라미터 그리드(2000억↔1000억, 3%↔5%) 히트맵. 가장 좋은 칸을 고르면 과최적화 — 넓고 완만한 고원을 고를 것.

## Tier 3 — 앞의 것들이 돌기 시작한 뒤

7. **아침 브리핑 한 장** — 전일 스캐너 + 유튜브 요약 + 밤사이 공시 + 보유 종목 뉴스를 08:00 KST 한 페이지로. `summaries/` 게시 패턴 그대로라 새 인프라가 없다.
8. **악마의 변호인 체크리스트** — 매수 직전 반대 논리 5개와 "내가 틀렸음을 알려줄 관측 가능한 신호 3개"를 강제로 받는다. 세 번째가 그대로 손절 조건이 되어 3번 일지와 맞물린다.
9. **포트폴리오 월간 검진** — 상관계수·섹터 비중·환노출은 코드로 계산, Claude는 "이 셋은 결국 같은 한 가지에 걸린 포지션"이라는 설명을 만든다.

## 쓰면 안 되는 곳

- 주가·목표가 예측 — 모르는 것을 그럴듯한 서사로 만들어 준다
- 실시간 시세·재무 수치 질의 — 시세는 언제나 pykrx·DART에서
- 수익률·비중·PER 계산 위임 — 모델은 계산기가 아니다
- 요약을 근거 확인 없이 매매 신호로 사용 — 요약은 압축이지 사실 검증이 아니다
- 자동 주문 연결 — 최소한 2번 스코어보드로 몇 달치 성적이 남기 전까지는 절대

## 모든 파이프라인 공통

- **고정 JSON 스키마** — 파싱 실패 시 1회 재시도 후 폐기. 누적 데이터가 되려면 형식이 고정돼야 한다
- **근거 인용 의무** — 인용 없으면 "근거 없음". 환각을 사후에 잡는 가장 싼 방법
- **비용 상한** — `max_videos_per_channel`과 같은 방식으로 종목 수·문서 길이 제한, 가벼운 분류는 effort 하향
- **성적 기록** — 판정마다 날짜·버전. 채점되지 않는 파이프라인은 몇 달 뒤 취미가 된다

## 4주 로드맵

| 주차 | 만드는 것 | 손에 남는 것 |
|---|---|---|
| 1주차 | 급등 사유 판별기(1) | 장 마감 후 자동 생성되는 "왜 올랐나" 표 |
| 2주차 | 유튜브 픽 추출(2 전반), 기존 요약 소급 처리 | 과거 언급 종목이 모인 picks.csv |
| 3주차 | 픽 채점기(2 후반) + 매매 일지 골격(3) | 채널별 적중률 첫 숫자, 진입 전 기록 습관 |
| 4주차 | 장대양봉 전략 백테스트(6) | 지금 쓰는 조건의 실제 기대값과 손익비 |

---

이 문서는 투자 워크플로 자동화를 위한 소프트웨어 설계 아이디어이며 투자 자문이나 종목 추천이 아니다.
모든 매매 판단과 결과의 책임은 투자자 본인에게 있다.
